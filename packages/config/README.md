# packages/config

**Status: contracts and conventions only — no implementation.**

Environment conventions (development / staging / production) and the config-loading contract, per `docs/architecture/deployment-portability.md` "Environment conventions". No real credentials anywhere in this package or repository.

- `src/environment.ts` — the `EnvironmentName` type and `ConfigProvider` contract.
