# infra/aws

**Status: documentation pointer only — no AWS resources provisioned in this PR.**

Future home of AWS deployment configuration (e.g. infrastructure-as-code) for `platform-services/*`, should AWS be chosen as a deployment target for some or all of the platform. See `docs/architecture/deployment-portability.md` "AWS" for the future-compatible component list (Route 53, CloudFront, WAF, ALB/API Gateway, ECS/Fargate, RDS PostgreSQL, S3, Cognito, Secrets Manager, KMS, CloudWatch). AWS is documented as a *possible* target, not a commitment — nothing here provisions anything.
