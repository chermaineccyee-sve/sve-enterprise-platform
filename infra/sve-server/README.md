# infra/sve-server

**Status: documentation pointer only — nothing provisioned or configured in this PR.**

Future home of SVE-private-server deployment configuration (reverse proxy, application runtime, PostgreSQL, private object storage, secrets, log shipping, backup/recovery) for `platform-services/*` once it exists. See `docs/architecture/deployment-portability.md` "SVE private/internal server" for the target component list — nothing here is provisioned yet.
