# infra/local

**Status: documentation pointer only — no local stack provisioned in this PR.**

Future home of local-development infrastructure (e.g. a `docker-compose.yml` for Postgres/Redis once `platform-services/*` needs them) — distinct from `apps/svegip`, which continues to run via `netlify dev` and needs nothing here. See `docs/architecture/deployment-portability.md`.
