# SVEGIP HRMS Application Shell & UI/UX Foundation (PR #11)

Status: **Draft — inspection in progress.** This file is a placeholder
opened alongside the Draft PR before implementation begins, per process
(the PR is opened as Draft before substantial UI work, and inspection
findings are reported before major implementation).

This document will be filled in with the full architecture write-up
(role of HRMS within SVEGIP, information architecture, personas,
navigation model, frontend/backend boundaries, responsive approach,
security model, existing backend capability mapping, API gaps, future
module extension points, explicit exclusions) once inspection of the
existing `apps/svegip` frontend and the backend capabilities of
`platform-services/organisation`, `platform-services/hrms`,
`platform-services/workflow`, and `platform-services/identity` is
complete.
