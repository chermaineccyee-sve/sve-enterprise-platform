{
  "name": "@sve/hrms-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SVE HRMS Employee Lifecycle foundation (onboarding/probation/employment-change/offboarding process backbone). Depends on platform-services/identity's Identity/RBAC/Audit contracts and platform-services/organisation's Employee Master/Organisation contracts (imported by source path — see README.md); does not duplicate their implementation, and neither package depends on this one.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "migrate": "node ../identity/scripts/migrate.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test --test-concurrency=1",
    "test:unit": "node --test test/unit",
    "test:integration": "node --test --test-concurrency=1 test/integration",
    "dev": "node src/server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "22.20.2",
    "typescript": "5.9.3"
  }
}
